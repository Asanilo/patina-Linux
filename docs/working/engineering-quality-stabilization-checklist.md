# 工程质量稳定期长期执行清单

## 1. 文档定位

本文是 `Time Tracker` 在当前稳定期使用的工程质量长期执行清单。

它服务于：

- [`../engineering-quality-target.md`](../engineering-quality-target.md)
- [`../code-quality-target.md`](../code-quality-target.md)
- [`../performance-target.md`](../performance-target.md)
- [`../reliability-and-validation-target.md`](../reliability-and-validation-target.md)
- [`../architecture.md`](../architecture.md)

它不是长期母文档，也不是纯说明文，而是一份可以逐项勾选、逐阶段推进、最终归档的执行清单。

它的作用是：

- 把“工程质量提升”拆成可落地的阶段任务
- 让仓库维护者随时知道当前推进到哪一阶段
- 让后续 Codex / GPT 协作者可以直接按清单推进，而不是每次重做全局判断
- 为后续沉淀长期 `engineering-quality.md` 母文档积累真实规则

如果本文与长期规则文档冲突，以 [`../architecture.md`](../architecture.md) 和 [`../engineering-quality-target.md`](../engineering-quality-target.md) 为准；本文应随之更新。

---

## 2. 使用方式

### 2.1 勾选规则

每个任务只有在下面条件都满足时才可以勾选：

- 代码已经落地
- 相关导入、调用路径、目录落点或脚本入口已经收口
- 没有引入新的边界回流
- 对应的最低验证已经完成
- 如有必要，相关文档也已经同步

### 2.2 执行粒度

本文故意把任务拆小。

默认原则是：

- 一次任务尽量只推进一个小阶段
- 一次任务尽量只解决一个清楚的质量问题
- 先补基线和保护，再做高风险整理
- 先解决真实热点，再解决普遍整洁
- 先保证可靠，再追求更快

### 2.3 允许调整

本文中的目标落点和建议文件，不是机械搬运规则，而是“推荐 owner 与推荐顺序”。

如果执行过程中发现：

- 更小的安全切分路径
- 更合适的 owner
- 更自然的 feature 边界
- 更适合前置的低风险工作

可以调整实现方式，但不要改变本清单的整体方向。

### 2.4 何时回写文档状态

出现下面情况时，应及时更新本文，而不是等很多轮之后一起回写：

- 某个阶段已经明显完成
- 某个热点已经自然收口
- 某个默认验证门槛发生变化
- 某个热点 owner 判断已经改变
- 某个阶段需要被拆成新的专项执行单

### 2.5 完成判断

“完成当前稳定期的工程质量执行”不等于仓库已经完美，而是至少满足：

- 仓库已经建立统一质量 gate
- 关键可靠性热点已经有硬保护
- 关键代码质量热点已经完成首轮有效收口
- 性能讨论已经建立在统一口径上
- 团队已经积累出足够稳定的长期规则

---

## 3. 当前阶段快照

下面这些事项已经基本成立，可以视为当前已完成的基础盘：

- [x] 已有工程质量总览文档 [`../engineering-quality-target.md`](../engineering-quality-target.md)
- [x] 已有三份专项长期目标文档：代码质量、性能、可靠性与验证
- [x] 已有当前 working 执行清单，可作为阶段性执行载体
- [x] 已有基础验证脚本：`npm test`、`npm run test:replay`、`npm run build`
- [x] 当前仓库已有长期架构母规则 [`../architecture.md`](../architecture.md)
- [x] 当前仓库已完成一轮较大规模的结构迁移，主要边界已比过去清晰

下面这些事项是当前仍未完成的关键工程质量里程碑：

- [ ] 仓库有统一的工程质量检查入口，例如 `npm run check`
- [ ] 已形成“改哪类风险，就默认跑哪组验证”的统一矩阵
- [ ] tracking 主链的关键不变量已被文档化并映射到验证动作
- [ ] backup / restore / cleanup 的数据边界风险已有更明确的默认验证
- [ ] `AppMapping.tsx`、`Settings.tsx`、`sessionReadCompiler.ts`、`runtime.rs`、`trackingLifecycle.test.ts` 等热点已有明确收口计划
- [ ] 性能讨论已有统一口径，而不是只凭主观感觉
- [ ] 当前执行完成后，已经具备评估是否沉淀长期 `engineering-quality.md` 的条件

### 3.1 当前默认高风险区

下面这些位置默认视为当前稳定期高风险区：

- [ ] `src/features/classification/components/AppMapping.tsx`
- [ ] `src/features/settings/components/Settings.tsx`
- [ ] `src/shared/lib/sessionReadCompiler.ts`
- [ ] `src/app/services/*`
- [ ] `src-tauri/src/engine/tracking/runtime.rs`
- [ ] `tests/trackingLifecycle.test.ts`
- [ ] `src-tauri/src/app/*`
- [ ] `src-tauri/src/commands/*`

### 3.2 当前默认验证基线

当前默认最低验证基线为：

- [x] `npm test`
- [x] `npm run test:replay`
- [x] `npm run build`

当前建议按风险追加的验证为：

- [ ] 涉及 Rust tracking 主链、数据边界或恢复路径时，追加 `cargo check`
- [ ] 涉及发布行为时，追加 release/changelog 相关验证
- [ ] 涉及性能热点时，追加前后对照测量

如果只想快速判断当前大概处于哪一步，先看这一节。

---

## 4. 执行顺序总览

本文默认按下面顺序推进：

1. 建立统一 gate 和事实基线
2. 先补 tracking 主链与数据边界的可靠性
3. 再收前端热点、共享热点和测试热点
4. 再收 Rust runtime 与壳层热点
5. 再建立性能口径并推进低风险性能改进
6. 最后做全局收口、文档收口和长期规则判断

这不是硬性 waterfall。
如果某次任务只触及其中一小块，可以直接推进那一块，但不要违背整体顺序的方向。

---

## 5. 阶段零：建立统一 gate 与事实基线

本阶段目标：

- 把“工程质量”从抽象词变成可重复判断的执行语言
- 给后续所有专项优化建立共同入口

### 5.1 建立统一质量入口

- [ ] 决定当前仓库的统一质量入口名称
- [ ] 决定统一入口是否落为 `npm run check`
- [ ] 如果不使用 `npm run check`，明确等效入口的名称和调用方式
- [ ] 让统一入口至少串起 `npm test`
- [ ] 让统一入口至少串起 `npm run test:replay`
- [ ] 让统一入口至少串起 `npm run build`
- [ ] 明确统一入口是否需要区分“前端变更”和“全栈高风险变更”

### 5.2 建立风险到验证的映射矩阵

- [ ] 明确前端热点页面改动默认跑哪些验证
- [ ] 明确 tracking lifecycle / replay 相关改动默认跑哪些验证
- [ ] 明确 tracker heartbeat / stale / cleanup 相关改动默认跑哪些验证
- [ ] 明确 backup / restore / cleanup 相关改动默认跑哪些验证
- [ ] 明确 settings 持久化与行为编排相关改动默认跑哪些验证
- [ ] 明确 release / updater 相关改动默认跑哪些验证
- [ ] 明确性能相关改动必须带前后测量依据

### 5.3 固化当前阶段首批热点清单

- [ ] 明确当前阶段首批代码质量热点文件
- [ ] 明确当前阶段首批可靠性热点链路
- [ ] 明确当前阶段首批性能热点场景
- [ ] 把高风险区写成稳定清单，而不是每次临时判断
- [ ] 把“什么情况需要暂停并升级”写成明确规则

### 5.4 建立工程事实基线

- [ ] 记录当前默认验证脚本及其用途
- [ ] 记录当前热点文件清单
- [ ] 记录当前默认高风险区
- [ ] 记录当前尚未建立的 gate
- [ ] 记录当前尚未建立的测量口径
- [ ] 记录当前明确不做的事情，避免这轮执行变成大重构

### 5.5 阶段验收

- [ ] 仓库已有统一质量入口或同等强度固定入口
- [ ] 风险到验证矩阵已经写清
- [ ] 热点名单已经固定
- [ ] 后续阶段的讨论已经共享同一套语言和门槛

完成本阶段后，后续工程质量优化不应再停留在“感觉上该优化”，而应转入“按哪类风险推进哪种动作”。

---

## 6. 阶段一：tracking 主链可靠性

本阶段目标：

- 先让“可被长期信任”成为工程事实
- 先补验证保护，再做深层整理和性能动作

### 6.1 文档化 tracking 主链不变量

- [ ] 列出 session 开始的关键条件
- [ ] 列出 session 结束的关键条件
- [ ] 列出同一 app / 同一窗口 / 同一 executable 的边界判断
- [ ] 列出 AFK 进入与退出时的预期行为
- [ ] 列出 startup sealing 的预期行为
- [ ] 列出 stale tracker 的预期行为
- [ ] 把这些不变量映射到现有测试或待补测试

### 6.2 生命周期主链保护

- [ ] 审视 `tests/trackingLifecycle.test.ts` 当前覆盖的行为
- [ ] 标出 lifecycle 已覆盖的关键场景
- [ ] 标出 lifecycle 仍未覆盖的关键场景
- [ ] 补齐切换、合并、过滤、别名归一化等高风险行为保护
- [ ] 确认测试重组不会削弱主链保护

### 6.3 replay 主链保护

- [ ] 审视 `tests/trackingReplay.test.ts` 当前保护范围
- [ ] 标出 replay 当前能验证的链路
- [ ] 标出 replay 当前验证不到的链路
- [ ] 明确 replay 更适合保护哪些变更类型
- [ ] 为 replay 相关高风险改动写清默认验证动作

### 6.4 heartbeat / stale / cleanup 行为保护

- [ ] 明确 heartbeat 过期的判断口径
- [ ] 明确 stale tracker 对 live session 展示的影响口径
- [ ] 明确 cleanup 何时只收尾，何时改变行为语义
- [ ] 为 heartbeat / stale / cleanup 相关改动补更直接的保护

### 6.5 阶段验收

- [ ] tracking 主链关键不变量已经写清
- [ ] lifecycle 与 replay 的保护边界已经写清
- [ ] 高风险 tracking 改动已经有固定验证组合
- [ ] cleanup / stale / heartbeat 不再明显依赖人工经验放行

完成本阶段后，tracking 主链不应再处于“大家大概知道它该怎么工作”的状态，而应进入“关键语义可被明确验证”的状态。

---

## 7. 阶段二：数据边界与恢复可靠性

本阶段目标：

- 让数据边界和恢复路径从“默认谨慎”升级成“默认可验证”

### 7.1 backup / restore 边界梳理

- [ ] 梳理 backup 的关键输入输出
- [ ] 梳理 restore 的关键输入输出
- [ ] 梳理 cleanup 与 backup / restore 的关系
- [ ] 梳理版本兼容和 schema 风险
- [ ] 梳理 restore 后需要触发的同步与刷新动作

### 7.2 数据边界默认验证

- [ ] 明确数据边界相关改动默认追加哪些验证
- [ ] 明确哪些数据边界改动仅跑前端脚本是不够的
- [ ] 明确哪些数据边界改动必须追加 `cargo check`
- [ ] 明确哪些数据边界改动必须人工验证恢复路径
- [ ] 明确人工验证只能作为补充，而不是唯一保护

### 7.3 settings / backup / restore 的协作约束

- [ ] 明确 settings 改动何时会影响 restore 行为
- [ ] 明确 backup / restore 改动何时会影响前端读模型
- [ ] 明确需要同时更新文档与验证矩阵的情况

### 7.4 阶段验收

- [ ] backup / restore / cleanup 的风险点已被系统梳理
- [ ] 数据边界改动已有固定追加验证动作
- [ ] 恢复路径不再依赖“改的人自己心里有数”

完成本阶段后，数据相关高风险改动应有比现在更硬的进入门槛。

---

## 8. 阶段三：前端热点页面收口

本阶段目标：

- 降低热点页面的认知负担
- 让复杂度稳定留在真实 owner 内

### 8.1 `AppMapping.tsx` 收口

- [ ] 梳理 `AppMapping.tsx` 当前承担的职责
- [ ] 区分页面壳层职责与 classification owner 逻辑
- [ ] 区分 UI 结构、交互状态、映射规则、持久化调用
- [ ] 找出可安全拆出的局部 UI
- [ ] 找出可安全拆出的 hooks
- [ ] 找出可安全拆出的 feature 内部服务
- [ ] 避免把 classification 私有逻辑重新塞到 `shared/*`
- [ ] 避免把 UI 层状态塞回 `app/*`
- [ ] 确认拆分后交互契约不变

### 8.2 `Settings.tsx` 收口

- [ ] 梳理 `Settings.tsx` 当前承担的职责
- [ ] 区分展示、状态编排、外部调用、持久化读写
- [ ] 找出可安全拆出的局部面板
- [ ] 找出可安全拆出的 hooks
- [ ] 找出可安全拆出的 settings owner 内部服务
- [ ] 避免把设置私有逻辑重新塞回 `shared/*`
- [ ] 避免把设置编排重新塞回 `app/*`
- [ ] 确认拆分后行为契约和用户操作路径不变

### 8.3 `Dashboard.tsx` / `History.tsx` 只做必要收口

- [ ] 审视 `Dashboard.tsx` 是否已有局部回流迹象
- [ ] 审视 `History.tsx` 是否已有局部回流迹象
- [ ] 只处理明显新增的高吸力问题
- [ ] 不把当前阶段扩张成前端全面重构

### 8.4 阶段验收

- [ ] `AppMapping.tsx` 结构明显更清楚
- [ ] `Settings.tsx` 结构明显更清楚
- [ ] 页面热点的复杂度没有转移成新的共享垃圾桶
- [ ] 相关行为保护在重构前后保持稳定

完成本阶段后，前端热点页面应从“能工作但很难安心改”进入“仍然复杂，但已经能分区理解”的状态。

---

## 9. 阶段四：共享层、壳层与兼容壳收口

本阶段目标：

- 防止 `shared/*`、`app/*`、兼容壳成为新的吸力层

### 9.1 `src/shared/lib/sessionReadCompiler.ts` 收口

- [ ] 审视该文件当前真实职责边界
- [ ] 区分共享 session 编译核心与 feature 私有读模型拼装
- [ ] 区分共享编译逻辑与 feature 私有 formatting
- [ ] 避免把页面语义回流到 shared
- [ ] 明确这个文件未来应保留的稳定职责

### 9.2 `src/app/services/*` 收口

- [ ] 梳理 `src/app/services/*` 当前名单
- [ ] 标出真正属于 app 编排的服务
- [ ] 标出更像 feature owner 的服务
- [ ] 标出更像 platform owner 的服务
- [ ] 收薄只因“方便”而停留在 app 层的逻辑
- [ ] 确保 `app/*` 继续回到壳层与跨 feature 编排身份

### 9.3 兼容壳与 legacy 转发层检查

- [ ] 清点当前仓库仍存在的兼容壳
- [ ] 判断每个兼容壳是否仍有现实价值
- [ ] 清楚标记哪些兼容壳是历史残留，不是新先例
- [ ] 保证兼容壳保持足够薄
- [ ] 阻止新需求继续落入兼容壳

### 9.4 阶段验收

- [ ] `shared/*` 不再承担新的过渡职责
- [ ] `app/*` 没有继续长成万能层
- [ ] 兼容壳有明确存在理由，且保持很薄

完成本阶段后，当前高吸力层应比现在更稳定，而不是继续变厚。

---

## 10. 阶段五：测试热点与验证组织收口

本阶段目标：

- 让测试文件本身也更可维护
- 让验证动作更像体系，而不是脚本列表

### 10.1 `tests/trackingLifecycle.test.ts` 收口

- [ ] 审视该测试文件当前是否过厚
- [ ] 区分不变量测试与场景测试
- [ ] 区分基础 helper 与具体场景数据
- [ ] 找出适合抽出的 fixtures
- [ ] 找出适合抽出的 helpers
- [ ] 找出适合拆分的场景组
- [ ] 保证拆分后仍然容易看出失败原因

### 10.2 验证动作组织收口

- [ ] 让“改哪类风险跑哪组验证”比现在更清楚
- [ ] 让默认验证动作和专项验证动作分层
- [ ] 让发布前验证与日常改动验证分层
- [ ] 让文档中的验证门槛能直接映射到仓库命令

### 10.3 阶段验收

- [ ] 测试热点本身更易读、更易维护
- [ ] 默认验证矩阵更清楚
- [ ] 发布前和日常改动的验证分层更清楚

完成本阶段后，验证体系应更像“可执行规则”，而不是“知道要跑几个命令”。

---

## 11. 阶段六：Rust runtime 与入口热点收口

本阶段目标：

- 防止 Rust 入口、runtime、commands 回流成新的厚层

### 11.1 `src-tauri/src/engine/tracking/runtime.rs` 收口

- [ ] 审视当前剩余复杂度主要集中在哪些职责
- [ ] 区分真正属于 runtime 编排的部分
- [ ] 区分仍适合继续下沉到 engine 子模块的部分
- [ ] 区分已经应交给 domain 的稳定语义
- [ ] 避免把 engine 逻辑重新推回 `app/*`、`commands/*` 或 `lib.rs`

### 11.2 `src-tauri/src/app/*` 与 `commands/*` 反回流检查

- [ ] 审视 `app/*` 是否有新的第二层业务实现
- [ ] 审视 `commands/*` 是否开始承担过多业务逻辑
- [ ] 审视 `lib.rs` 是否有回胖迹象
- [ ] 如果发现回流，优先把逻辑移回正确 owner

### 11.3 阶段验收

- [ ] `runtime.rs` 复杂度继续下降或至少不再回升
- [ ] `app/*`、`commands/*`、`lib.rs` 保持薄
- [ ] Rust 入口层仍保持清晰装配身份

完成本阶段后，Rust 热点应继续向长期结构靠拢，而不是回到迁移前状态。

---

## 12. 阶段七：建立性能口径

本阶段目标：

- 让性能讨论建立在统一口径上
- 让后续性能动作不再停留在主观印象层

### 12.1 启动与首屏口径

- [ ] 定义冷启动口径
- [ ] 定义热启动口径
- [ ] 定义“首屏可用”的判断口径
- [ ] 记录当前冷启动基线
- [ ] 记录当前首屏体验基线
- [ ] 记录 startup prewarm 的现状
- [ ] 记录 snapshot cache 的现状

### 12.2 读模型与刷新口径

- [ ] 梳理 dashboard 读模型重算触发点
- [ ] 梳理 history 读模型重算触发点
- [ ] 记录哪些场景会导致高频重算
- [ ] 记录当前缓存是否真的减少开销
- [ ] 确定哪些指标最值得作为首批观测口径

### 12.3 后台资源口径

- [ ] 梳理 tracking runtime 轮询节奏
- [ ] 梳理 heartbeat / health polling 节奏
- [ ] 记录空闲时 CPU 观察口径
- [ ] 记录空闲时内存观察口径
- [ ] 记录高频窗口切换时的资源观察口径

### 12.4 SQLite 与持久化口径

- [ ] 梳理热点查询路径
- [ ] 梳理高频写入路径
- [ ] 梳理可能的重复读取
- [ ] 梳理可能的重复写入
- [ ] 记录首批最值得观测的数据库热点

### 12.5 阶段验收

- [ ] 启动、读模型、后台资源、SQLite 都已有基础口径
- [ ] 性能讨论不再只凭感觉
- [ ] 后续性能优化已经可以建立在对照事实之上

完成本阶段后，性能讨论应具备“可以被反复使用”的共同语言。

---

## 13. 阶段八：推进低风险性能改进

本阶段目标：

- 只做有依据、可解释、低风险的性能改进

### 13.1 启动与首屏低风险优化

- [ ] 审视 prewarm 是否存在无效预热
- [ ] 审视 snapshot cache 是否存在无效刷新
- [ ] 只保留对首屏真实有帮助的预热
- [ ] 避免为了“感觉更快”引入高复杂度特例

### 13.2 读模型与刷新低风险优化

- [ ] 优先处理可验证的重复重算
- [ ] 优先处理可验证的无效刷新
- [ ] 保证优化不会破坏读模型语义
- [ ] 保证优化前后有可比对的测量依据

### 13.3 后台资源低风险优化

- [ ] 优先处理空闲时可见的无效轮询
- [ ] 优先处理明显不必要的后台刷新
- [ ] 保证轮询调整不会伤害可靠性
- [ ] 如有取舍冲突，优先保可靠性

### 13.4 SQLite 低风险优化

- [ ] 优先处理用户可感知的查询浪费
- [ ] 优先处理明显重复的高频读取
- [ ] 优先处理明显重复的高频写入
- [ ] 不把数据库热点伪装成“顺手抽象一下”

### 13.5 阶段验收

- [ ] 已完成至少一批低风险性能改进
- [ ] 改进前后有基本对照依据
- [ ] 没有为了更快明显伤害结构质量或可靠性

完成本阶段后，性能优化应开始是“有依据的改善”，而不是“可能更快的猜测”。

---

## 14. 阶段九：发布前质量门槛收口

本阶段目标：

- 让发布前验证门槛不再模糊

### 14.1 默认发布门槛

- [ ] 明确当前阶段的默认发布最低门槛
- [ ] 明确哪些情况只跑前端基线不够
- [ ] 明确哪些情况必须追加 `cargo check`
- [ ] 明确哪些情况必须追加 release/changelog 相关验证
- [ ] 明确不满足门槛时不能视为可发布

### 14.2 日常改动与发布改动分层

- [ ] 明确日常低风险改动的默认验证动作
- [ ] 明确高风险改动的默认验证动作
- [ ] 明确发布前验证与日常验证的差异

### 14.3 阶段验收

- [ ] 发布前最低门槛已经写死
- [ ] 日常与发布验证分层已清楚
- [ ] 高风险改动不会再轻易以低门槛放行

完成本阶段后，发布前判断应比现在更明确、更稳定。

---

## 15. 阶段十：文档与长期规则收口

本阶段目标：

- 检查当前执行是否真正形成了稳定经验
- 判断是否具备沉淀长期母文档的条件

### 15.1 专项文档是否已形成稳定规则

- [ ] 代码质量文档已不再承载强阶段性内容
- [ ] 性能文档已不再承载强阶段性内容
- [ ] 可靠性与验证文档已不再承载强阶段性内容
- [ ] 总览文档中的优先级与取舍方式已稳定

### 15.2 执行清单是否具备归档条件

- [ ] 本清单中的主要任务已有真实落地结果
- [ ] 本清单中的热点判断已基本稳定
- [ ] 本清单不再需要继续扩张成长期 backlog
- [ ] 已具备归档并转入长期母文档的条件

### 15.3 阶段验收

- [ ] 团队已经积累出可复用的默认规则
- [ ] 已能清楚判断是否开始起草 `docs/engineering-quality.md`
- [ ] 本清单已接近可以归档的状态

完成本阶段后，工程质量文档体系应从“识别期”进入“母规则沉淀期”。

---

## 16. 最终收口检查

本阶段目标：

- 用一轮清点确认当前稳定期目标已经真正落地

### 16.1 可靠性与验证收口检查

- [ ] tracking 主链关键不变量已文档化
- [ ] tracking 主链关键不变量已有验证映射
- [ ] replay 与 lifecycle 的保护边界已清楚
- [ ] backup / restore / cleanup 的默认验证动作已清楚
- [ ] 发布前最低门槛已清楚

### 16.2 代码质量收口检查

- [ ] 前端热点页面已有有效收口
- [ ] `shared/*`、`app/*`、兼容壳没有继续回流
- [ ] 测试热点本身更可维护
- [ ] Rust runtime 热点没有回胖

### 16.3 性能收口检查

- [ ] 启动与首屏已有基础口径
- [ ] 读模型与刷新已有基础口径
- [ ] 后台资源已有基础口径
- [ ] SQLite 热点已有基础口径
- [ ] 已完成至少一批低风险性能改进

### 16.4 文档收口检查

- [ ] 当前总览文档与专项文档状态一致
- [ ] 本执行清单中的阶段状态与仓库现状一致
- [ ] 如果已有长期母文档准备动作，相关文档关系已写清

---

## 17. 执行时的默认约束

- 不为了勾选速度做大爆炸重构
- 不为了“整洁感”牺牲可靠性保护
- 不为了“更快”制造高复杂度特例
- 不把 `shared/*` 做成新的垃圾桶
- 不把 `app/*` 做成新的万能层
- 不把 `platform/*` 做成新的难题倾倒区
- 不把兼容壳当作新的正式落点
- 不把文件移动完成误判为职责已经收口
- 一次任务只推进一小步，但每一步都必须让结构、验证或口径更清楚

---

## 18. 给后续协作者的说明

如果以后继续基于本文推进，请默认遵守下面这些规则：

- 优先完成一个清楚的小阶段，再勾选对应条目
- 不要一次跨越多个阶段同时大改
- 如果执行中发现 owner 判断与本文不一致，先修正文档再继续
- 如果某个热点已经自然完成收口，应及时回写状态
- 如果某个热点扩张成独立专项，应暂停当前条目，改为新的专项执行单

本文的目标不是“看起来任务很多”，而是让工程质量这条长期方向始终看得见、推得动、对得上当前阶段。
